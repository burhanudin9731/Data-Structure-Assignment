10
       /  \
      20   30
     /  \    \
    40  50   60

| Index | 0  | 1  | 2  | 3  | 4  | 5 | 6  |
| ----- | -- | -- | -- | -- | -- | - | -- |
| Value | 10 | 20 | 30 | 40 | 50 | - | 60 |


#include <stdio.h>

int main() {
    int tree[10];

    tree[0] = 10;   // Root
    tree[1] = 20;   // Left child of root
    tree[2] = 30;   // Right child of root
    tree[3] = 40;   // Left child of 20
    tree[4] = 50;   // Right child of 20
    tree[6] = 60;   // Right child of 30

    printf("Tree elements stored in array:\n");
    for(int i = 0; i < 7; i++) {
        if(tree[i] != 0)
            printf("Index %d : %d\n", i, tree[i]);
    }

    return 0;
}

OUT-OUT:
Index 0 : 10
Index 1 : 20
Index 2 : 30
Index 3 : 40
Index 4 : 50
Index 6 : 60
